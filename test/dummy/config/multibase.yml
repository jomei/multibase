sqllite_db:
  default: &default
    adapter: sqlite3
    pool: 5
    timeout: 5000

  development:
    <<: *default
    database: db/development.sqlite3

  test:
    <<: *default
    database: db/test.sqlite3

  production:
    <<: *default
    database: db/production.sqlite3

postgre_db:
  development:
    adapter: postgresql
    encoding: utf8
    database: multibase_db
    username: multibase
    password: multibase
    host: 127.0.0.1

  test:
    adapter: postgresql
    encoding: utf8
    database: multibase_db
    username: multibase
    password: multibase
    host: 127.0.0.1
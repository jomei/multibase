require "spec_helper"

describe Multibase do
  it "has a version number" do
    expect(Multibase::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
